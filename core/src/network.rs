// use crate::{message::{IncomingMessage, OutgoingMessage}, peer::PeerId};

// pub trait NetworkParticipant {
//     fn peer_id(&self) -> PeerId;

//     fn handle_incoming(&mut self, msg: IncomingMessage);

//     fn send(&self, msg: OutgoingMessage);
// }